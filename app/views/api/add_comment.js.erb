<% comment = @comment%>
<% option = comment_hash(comment, @username, @user_email)%>
<%= juvia_handle_response(
  :status         => 'ok',
  :action         => 'AddComment',
  :site_key       => @site_key,
  :topic_key      => @topic_key,
  :comment_option => option
) %>
