<% comment = @comment%>
<% option = comment_hash(comment, comment.author_name, comment.author_email,request.remote_ip)%>
<%= juvia_handle_response(
  :status         => 'ok',
  :action         => 'AddComment',
  :site_key       => @site_key,
  :topic_key      => @topic_key,
  :comment_option => option
) %>
