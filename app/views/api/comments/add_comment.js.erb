<% comment = @comment%>
<% option = comment_hash(comment, comment.author, {:topic_url => @topic.present? ? @topic.url : "" })%>
<%= juvia_handle_response(
  :status         => 'ok',
  :action         => 'AddComment',
  :site_key       => @site_key,
  :topic_key      => @topic_key,
  :comment_option => option
) %>
