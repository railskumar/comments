<% comment = @comment%>
<% 
  option = comment_hash(comment, @username, @user_email)
%>
<%= {
  :status    => 'ok',
  :action    => 'AddComment',
  :site_key  => @site_key,
  :topic_key => @topic_key,
  :comment_option => option
}.to_json.html_safe %>
