<%= {
  :status    => 'error',
  :message   => 'Site key not found.',

  :action    => 'ShowError',
  :container => @container,
  :site_key  => @site_key,
  :topic_key => @topic_key,
  :html      => render(:partial => 'api/site_not_found', :formats => [:html], :locals => { :topic => @topic })
}.to_json.html_safe %>
